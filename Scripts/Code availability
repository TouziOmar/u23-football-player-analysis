The repository includes the main scripts unsed to reproduce the analytical steps of the project.Some scripts used during the development phase were not found fully preserved
So the code has been simplified to reflect the main methodology
